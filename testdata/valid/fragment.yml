apiVersion: ai-resource/draft
kind: Prompt
metadata:
  id: fragment-test
  name: Fragment Test
spec:
  fragments:
    greet:
      inputs:
        name:
          type: string
          required: true
      body: "Hello, {{name}}!"
  body:
    - fragment: greet
      inputs:
        name: "World"
